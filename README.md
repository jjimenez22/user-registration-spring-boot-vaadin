# user-registration-spring-boot-vaadin
A simple editable list of users written in Spring Boot Vaadin with h2 persistence and Maven.

Usage:
```
$ mvn spring-boot:run
```
Then browse to `localhost:8080`